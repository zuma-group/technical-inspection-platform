# Database Configuration
# Format: postgresql://[username]:[password]@[host]:[port]/[database]?schema=public
# 
# For local development with pgAdmin 4:
# 1. Username: Usually "postgres" (default PostgreSQL superuser)
# 2. Password: The password you set when installing PostgreSQL
# 3. Host: "localhost" for local database
# 4. Port: "5432" (default PostgreSQL port)
# 5. Database: The name you gave when creating the database in pgAdmin
#
# For production (Vercel/Supabase/Neon):
# Use the connection string from your database provider
# DATABASE_URL should use the pooled connection string
# DATABASE_DIRECT_URL should use the direct connection string
DATABASE_URL="postgresql://postgres:your_password_here@localhost:5432/your_database_name?schema=public"
DATABASE_DIRECT_URL="postgresql://postgres:your_password_here@localhost:5432/your_database_name?schema=public"

# Server Port (optional, defaults to 3000)
# Change this if port 3000 is already in use
PORT=3000

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"

# Generate a secure secret with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="generate_a_32_character_secret_here"